/**
 * Gemini API client — the ONLY module allowed to call the Gemini SDK.
 * All other code must import from here rather than calling the SDK directly.
 *
 * Placeholder: actual client setup will be added when business logic is built.
 */

export {};
